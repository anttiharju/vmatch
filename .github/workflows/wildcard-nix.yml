permissions:
  contents: none
  pull-requests: none
on:
  push:
    branches:
      - wildcard
    paths:
      - ".release/nix/**/*"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/wildcard-nix.yml"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true
