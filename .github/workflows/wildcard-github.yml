permissions:
  contents: none
  pull-requests: none
on:
  push:
    branches:
      - wildcard
    paths:
      - ".release/archive.sh"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/wildcard-github.yml"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true
