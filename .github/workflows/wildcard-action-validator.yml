on:
  push:
    branches:
      - wildcard
    paths:
      - ".github/workflows/*.yml"
      - "**/action.yml"

jobs:
  wildcard:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: |
          true
