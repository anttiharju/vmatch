permissions:
  contents: none
  pull-requests: none
on:
  push:
    branches:
      - wildcard
    paths:
      - "docs/**/*"
      - "mkdocs.yml"
      - "flake.nix"
      - "flake.lock"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true
