permissions:
  contents: read
  pull-requests: read
on:
  push:
    branches:
      - wildcard
    paths:
      - "**/*.sh"
      - "**/*.bash"

jobs:
  wildcard:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: |
          true
