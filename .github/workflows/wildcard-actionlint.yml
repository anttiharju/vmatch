on:
  pull_request:
    branches:
      - wildcard
    paths:
      - ".github/workflows/*.yml"

jobs:
  actionlint:
    runs-on: ubuntu-latest
    if: false
    steps:
      - name: actionlint
        run: actionlint -color

      - name: actionlint
        uses: anttiharju/actions/actionlint@v0
