on:
  pull_request:
    branches:
      - wildcard
    paths:
      - ".github/workflows/*.yml"

jobs:
  lefthook-job:
    runs-on: ubuntu-latest
    if: false
    steps:
      - name: actionlint
        run: actionlint -color
