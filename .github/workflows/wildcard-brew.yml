permissions:
  contents: none
  pull-requests: none
on:
  push:
    branches:
      - wildcard
    paths:
      - ".release/brew/**/*"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/wildcard-brew.yml"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true
